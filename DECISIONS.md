# 결정 기록

## 2026-09-24 무개입 실행과 설계 정합성

- 사용자 지시를 근거로 10/5 이전 사람 확인은 사전 위임 처리한다. 공식 자료 확인은 자동 수행하되 설문 응답·계약정보·제출 성공을 만들어 내지 않는다.
- 초기 브랜치를 최신 master a1ddbbd로 fast-forward. 기존 시제품·문서·verification은 보존한다. 원본 task05 CSV/ZIP을 바이트 복사했고 manifest에서 검증한다.
- 원점은 종료 직후, 특징 관측 ≤ 원점. 분할 경계의 학습 목표 < 다음 원점. 원문 9.4절 '<'와5절 y(t) 충돌 해소.
- 관리 목표 T는 본문 기본 τ=.95. 예시 YAML .975는 민감도용으로 정정.
- 최종 테스트 날짜 10/1 18시 KST는 유지. 지금은 개발 CV와 보고서 초안을 생성한다. 사전 동결 승인은 위임되었으나 날짜를 임의로 앞당기지 않는다.
- 같은 검증 표본에서 conformal 적합·선택·성능보고를 하지 않도록 폴드 내 보정과 점수 구간을 분리한다. 시계열 교환가능성이 없어 유한표본 커버리지 보장은 주장하지 않는다.
- CBL은 별표28의 값 정렬·절사 평균 방식 준용. 거래 감축 이력·정산 옵션 부재, 15분 해상도와 관측 가능성 제한의 차이를 명시한다.
- 저녁 경보 이후 지난 낮으로 이동하는 안은 사후 가정이다. 운영안은 준비시간 이후 목적지로만 제한하고 조치 불가 비율을 보고한다.
- 전체 계약종별·선택요금 미확인: 단가 null, 시간대·순서가중 시나리오만. 2026 개편은 계절·요일별로 분리하며 2021 자료에 정책을 재적용하는 반사실 분석이다.
- LightGBM 소규모 탐색: leaves15/min_child40,leaves31/min_child80; learning_rate .05, 최대600회, 조기종료60. 피크가중1·2·4, h1·4·16·96. 실제 시행수는 실행 manifest.
- 선택 기능 파운데이션·확률B·PDP/ICE 기본 비활성. mandatory 작업을 우선한다. 채택 기준은 개발 실행 전에 outputs/logs/adoption_criteria.md에 고정.
- Claude2R 교차검토를 요청한 패킷은 docs/decisions/claude_packet.md. wrapper가 exit1을 반환하여 검토 의견은 받지 못함. 합의·반론을 추정하지 않는다. 독립 코드 검토로 진행한다.

## Claude 시도 결과의 정리

- 최종 판단: 시간 누수·보정 평가 중복·불가능한 과거 이동을 보완한 설계로 구현한다.
- GPT와 Claude의 합의: 검토 실패로 없음.
- Claude의 핵심 반론: 수신하지 못함.
- 반론을 반영해 변경된 부분: 없음. 위 수정은 GPT 자체 검토다.
- 남은 이견: 외부 의견 미수신으로 평가 불가.
- Decision Boundary: 원점·단위·요금 계약 확인 시 가정만 갱신; 테스트 잠금 이후 모델 변경 금지.
- 추가 검증: 누수·에피소드·CBL·보정 테스트, 개발 CV, 독립 코드 검토.

## 개발 결과와 독립 검토 후 확정 사항

- h1은 최근값, h4는 공휴일 제외·피크 가중2 LightGBM, h16/h96은 휴일 혼합 CBL을 선택했다. 모든 거리의 위험 상단은 보정B를 사용한다. 기준선이 이기는 거리에 복잡한 점예측을 강제하지 않는다.
- h4 피크 MAE는14.4431, CBL18.2561, persistence34.2588이다. CBL 대비 개선 CI[-0.9260,9.8691]가0을 포함하므로 양쪽 기준선 대비 유의 개선이라는 필수 성공 목표는 개발 단계에서 미입증이다.
- 원시 분위수 행에 공통 CSV의 빈 보정 열이 존재해 원시 커버리지를 누락하던 평가 오류를 수정했다. 원시12행120개 지표 셀만 바뀌었고 전체 재선정 결과는 동일했다. 수정 코드로 독립 폴더에서 캐시 없는 재실행을 검증한다.
- FVA의 기각 후보는 다음 단계의 기준으로 넘기지 않는다. 분위수 보정의 가치는 커버리지·pinball로 비교하고 점예측 MAE 개선으로 포장하지 않는다.
- 저녁 위험 경보의 미탐 에피소드7건 중5건은 긴 경보가 다른 실제 피크에 먼저 일대일 배정되어 생긴 미매칭이다. 실제 겹침이 없는2건과 구분한다. 점예측 경보, 확률 경보, 준비시간 적용 운영 경보의 지표를 혼용하지 않는다.
- 의사결정 피드는 모든 예측거리를 유지하되 h4/h16에만 운영 경보를 정의한다. 다른 거리의 `alert_level`은 공란이며 `action_applicable`, `risk_exceeded`로 구분한다. 신호 미관측도 정상0으로 처리하지 않는다.
- 기대 초과량의 에피소드 최대값 비교는 순위·크기 대리지표다. 개별 시각 기대값을 에피소드 최대의 보정된 기대값이라고 주장하지 않는다.
- 최종 평가 예약은 현재 계정의 Interactive 작업이다. S4U 등록이 허용되지 않아 로그인한 세션과 가용한 PC가 필요하다. 예약 사실과 실제 실행 완료를 구분한다.
- 발표자료는 개발 결과14장 HTML·발표자 메모·PDF로 제공한다. 로컬 PPTX 스킬의 필수 `load_workspace_dependencies`와 지정 런타임이 없어 PPTX는 생성하지 못했다. `Do not install substitutes` 규칙에 따라 다른 PPTX 엔진을 임의 설치하지 않았다. HTML은 수정 가능한 원본이며 PDF를 PPTX라고 표시하지 않는다.
- CPU 전체 재현 목표30분에 맞춰 선택적 τ90/97.5 재학습과 T97.5 운영 민감도는 이번 실행에서 생략했다. YAML의 `peak.sensitivity`는 계획값이며 산출 완료를 뜻하지 않는다. 파운데이션·확률 분류B·PDP/ICE도 미실행이며, 네 예측거리·기대초과량·이동비율10/20/30%·피크 시점/크기 분석은 유지했다.

## 2026-09-24 7시간 무인 작업: 사전 결정

- 사용자 최신 지시에 따라 이번 세션은 개발 OOF 분석만 수행한다. 기존 전체 run_all.py는 원자료 전체를 읽으므로 호출하지 않고 테스트 경계 이전만 반환하는 별도 로더·부분 실행 스크립트를 사용한다.
- 기존 평가 프로토콜과 채택 기준은 보존한다. 새 규칙은 outputs/logs/adoption_criteria_addendum_0924.md에 먼저 커밋했고 이후 수정하지 않는다. 선택 작업 A/B/C도 결과 전 함께 등록하여 사후 수정 위험을 없앴다.
- 새 지표는 선정에 사용하지 않는다. 요금은 기존 순서형 가중치 시나리오만 사용하고 실제 단가·공식 비용으로 해석하지 않는다.
- 공휴일 공식 확인·주최측 답변·설문·10/1 동결 승인·10/2 요금 입력·PPTX 제작은 미완료이다. 이전 기록의 동결 승인 위임은 이번 인계의 완료 항목으로 취급하지 않는다.
- 기존 보호 산출물은 수정하지 않는다. 이번 실행 결과는 코드·테스트·outputs/tables 및 outputs/logs와 이 기록·PROGRESS에만 추가한다.
## 2026-09-24 task 1 — h96 진단 판정

- 판정은 **버그 아님**이다. 12개 거리·폴드의 적합/조기종료/보정/평가 경계와 행 수가 기존 manifest와 일치했다. 20개 원점을 직접 확인했고 목표=원점+24시간, 전일 목표 슬롯=현재 원점, 전주 목표 슬롯=원점-6일, lag672=원점-7일의 구분이 맞았다. 결측·복원 의존 행을 제외한 동일 OOF에서 대조했다.
- 사용자 현상의 MAE 31.2는 `lgbm_no_holiday`(31.207801)이다. 공휴일 포함 `lgbm`은 32.374404로 별도 후보이며 두 값을 혼용하지 않는다.

| h96 후보/대조 | 전체 MAE | 피크 MAE | 에피소드 F1 | 역할 |
| --- | ---: | ---: | ---: | --- |
| 기존 lgbm_no_holiday | 31.207801 | 41.720845 | 0.223048 | 질문에 해당하는 기존 후보 |
| 기존 lgbm (공휴일 포함) | 32.374404 | 42.044708 | 0.333333 | 전체 특징 대조 기준 |
| A 전주 목표 슬롯 그대로 | 19.844165 | 20.241192 | 0.428224 | s2_week와 동일 |
| B 네 특징 LightGBM | 25.128414 | 30.845102 | 0.443243 | 진단 전용, 선정에 미사용 |

- 전체 특징 모델 대비 피크 MAE 개선은 A 21.804 [11.269,30.488], B 11.200 [4.325,17.674]이다. 동일 피크 369위치의 날짜 블록 쌍 비교이며, 최종 모델 채택 판정으로 사용하지 않는다.
- h96 적합 표본은 5,446/8,805/12,165행, 마지막 원 검증 4,200행 중 유효 3,050행이다. 복원 의존 마스킹은 기준선·LightGBM에 동일 적용됐다. 전체 모델의 조기종료 반복 598/43/74, 공휴일 제외 600/43/59, 네 특징 모델 205/70/81로 폴드 차이가 크다. 특징을 포함했다고 그 값을 그대로 통과시키는 해가 자동 선택되는 것은 아니다. 네 특징 축소의 개선은 복잡도·시기별 일반화 문제 가설을 지지하지만 단일 원인을 입증하지 않는다. 표본 손실·조기종료·분포 변화의 영향을 인과적으로 분리하지 않았다.
- 예측 모델·특징·선정 규칙은 수정하지 않았고 h4 재선정/재학습도 필요하지 않다. 기존 h96 CBL 선정은 유지한다. 근거는 outputs/tables/h96_diagnostic_*.csv 및 outputs/logs/h96_diagnostic_0924.json이다.
- 작업1 캐시 없는 확인: 기존 full/공휴일 제외 설정을 3폴드씩 총 6개 새로 학습했다. 저장 OOF와 예측 최대 절대차 2.84e-14, 조기종료 반복 수 동일. 단순히 저장 모델을 다시 읽은 결과만으로 재현을 주장하지 않는다. 안전 로더·전용 진단 테스트를 통과했다.
- 조기종료 추가 확인: `eval_metric=l1`이어도 LightGBM 회귀 기본 l2가 함께 기록되고 `first_metric_only=False`이므로 두 지표를 감시한다. full fold1은 best=43/L1 최소=83/L2 최소=43, 공휴일 제외 fold1은43/77/43, fold2는59/78/59이다. 기존 곡선은 검증 L1만 그리므로 이것만으로 정지 원인을 단정하지 않는다. 이중 지표 감시는 구현의 현재 동작이며 사전 버그 판정 조건(정렬·결측·목표 불일치)에 해당하지 않아 학습 코드를 바꾸지 않았다. 근거: h96_diagnostic_early_stopping_metrics.csv. 원본 OOF timestamp 검사에는 반복 시각의 유효성만 메모이제이션하여 값 접근 규칙을 유지하면서 부분 재실행 비용을 줄였다.
## 2026-09-24 task 2 — 보조 지표와 CI

- `peak_mae_union`, `peak_bias`, `overpredict_rate`를 추가했다. 공집합 평균은 NaN으로 두고 분모·유효 부트스트랩 수를 표에 기록한다. 기존 선정 함수가 사용하는 지표와 규칙은 그대로이다.
- T2-3은 4거리×21후보=84행, 그중 분위수 q50 참고행12개이다. 전 행에 `selection_used=False`, 분위수 행에 `reference_only=True`를 쓴다. 새 union/과대예측은 pred>tau, 기존 위치 오경보·에피소드는 기존 보정 alert 기준이다.
- 날짜 블록1,000회 CI는 동일 날짜 재표집을 지표별로 공유한다. 자정을 가로지르는 원래 에피소드 매칭을 보존하고 TP/FN은 실제 에피소드 시작 날짜, FP는 경보 시작 날짜에 귀속한다. 날짜를 이어 붙여 가짜 에피소드를 만들지 않는다. 기존 선정용 CI 구현은 변경하지 않았으며 신규 표의 사건 시작일 클러스터 방식은 선정에 미사용이다.
- h4 선정 모델 union MAE=14.195703, CI[12.304145,16.614443]; 피크 편향=−13.787058, CI[−16.770799,−11.369116]; 비피크 과대예측률=.012068, CI[.006969,.018043]. 편향이 음수로 남았다는 실패 진단이며 추가 보정을 채택한 결과가 아니다. CBL 대비 주 지표의 유의 개선 미입증이라는 기존 결론도 바꾸지 않는다.
## 2026-09-24 task 3 — 표 분리와 별칭 통합

- FVA를 점예측 7행(T2-1a)과 불확실성 3행(T2-1b)으로 분리했다. 기존 T2-1 원문과 해시는 보존하고 logs의 DEPRECATED 메모에 대체 경로를 기록했다.
- c3_holiday_mid_4_6과 c3_holiday_hybrid의 키·예측·결측·경보는 4거리 모두 완전 동일했다. 새 T2-3 표시에서는 canonical hybrid 한 개만 유지한다. 원본 OOF와 기존 선정 ID는 보존한다. T2-3 최종 행 수는 80, q50 참고행은 12이다.
- 별칭 제거 전후 선정 전체를 재계산해 일치함을 확인했다: h1=p1_latest, h4=lgbm_no_holiday_weight_2, h16/h96=c3_holiday_hybrid. 최초 JSON 완전 비교는 CSV 왕복 부동소수점 차이로 실패했다. 사전 고정 수치 허용오차 1e-10으로 비교해 최대 차이 1.07e-14, 범주·행 수·선정 불일치 0건을 확인했다. 기준이나 선정 알고리즘은 변경하지 않았다.
- 관련 CBL/별칭/선정 비교 회귀 테스트 6개 통과. 독립 검토에 따라 학습 코어의 후보 행 제거는 적용하지 않았다. 사전 등록한 표시 행 통합만 수행하여 원본 OOF 재생성의 후보 ID·행 수를 보존한다. 예측 특징이나 학습 파라미터는 바꾸지 않는다.


## 2026-09-24 task 4 — 지연 이동·감축 v2

- 사전 등록대로 D(지연 이동)와 C(감축 상한 참고)를 10/20/30% × 준비15/30/60분으로 실행했다. 기존 h4 운영 1/1 경보(p>0.1 또는 q95>tau)를 유지했다. F1 최적화 경보와 혼용하지 않는다.
- D의 기본20%/30분은 경보1,293위치 중 출발343시간이 유효했고330시간을 이동했다. 조치 비율25.522%의 분모는 중복 출발시간을 포함한 전체 경보위치이다. 이동량 합의 차이7.67e-13으로 보존되었으며 새 피크0위치이다. C는343시간/26.527% 적용했다.
- D 월별 최대 변화(4~8월)는 −4.994/−2.072/−0.803/−3.593/0.000이고, 요금 시간대 가중 사용량 변화는 −430.888/−333.601/−708.504/−1192.133/0.000이다. 단위 미확인·순서형 가중치의 상대값이며 서로 다른 지표다. 평가 관측 비율이 월 전체의27.1~84.4%이므로 실제 월 최대수요 또는 요금적용전력 감소라고 주장하지 않는다.
- 모든 출력은 '사후 관측 생산량 기반 사후 가정 시나리오'로 표시했다. 같은 폴드 OOF 안에서 준비시간·비중첩·24시간 창·더 낮은 가중치·도착 tau 이하를 모두 검사한다. 최초 실패한 출발시간은 이후 유효 경보에서 재시도할 수 있고 성공한 시간만 중복 조치를 막는다.
- 준비60분은 h4 목표가 속한 1시간 출발 창 시작에 도달하지 못해 적용0이다. 15분 목표 종료까지의 경보 선행시간과 1시간 작업창 시작까지의 준비시간은 다르다. 실제 운전 제약이나 인과 효과는 검증하지 않았다. 관련11개 테스트 통과.

## 2026-09-24 task 5 — 설명 비율 기반 피크 유형 v2

- 학습 요일유형·15분 위치 중앙값 대비 excess와 Ridge 상승분으로 share를 계산했다. 경계0.2/0.5와 비양수 초과분 처리, 학습 셀 대체 순서는 사전 규칙 그대로이다.
- 통합 OOF R²=0.451909로 문턱0.2 통과; 폴드별0.345749/0.469711/0.451766이다. 경계 직전 미완료 시간의 생산량2행은 임의 보충하지 않고 Ridge 평가에서 제외(7,549행 평가)했다.
- 생산량/4 특징의 계수는0.097728 [0.062740,0.134487], 0.089215 [0.061438,0.117552], 0.089255 [0.064642,0.113185]로 모두 양수다. 원 시간당 생산량1단위의 15분 예측 관련량은 이 계수의1/4이다. 날짜 블록1,000회 학습 재적합 CI이며 인과 계수가 아니다.
- 148에피소드 중 생산 기인형60(40.54%, CI29.58~53.39%), 혼합18(12.16%, CI7.46~16.67%), 잔차70(47.30%, CI33.54~60.14%)이다. 명칭의 생산 기인은 생산량·기상·달력이 포함된 전체 기준선 설명형이며 생산량 단독 귀속이나 조치 효과가 아니다. 이전 규칙과의 분포 차이는 분류 정의 변경의 결과다.
- 대표3건은 사전 선택법대로 각 유형 최대 excess 사례: 7/26 19:30 share0.503967(excess41,rise20.662640), 7/27 19:45 share0.361553(49,17.716101), 5/9 09:00 share0(161,0). _v2 표에 원 수치·학습 셀 출처·R² 문턱 판정을 함께 기록했다. 기존 모델 선정은 유지한다.
- 독립 검토 보충: 기본 준비30분에서 시간·OOF창 조건을 통과한 경보행은577/1,293=44.625%, 이를 고유 출발창으로 합치면343개이다. 330/1,293=25.522%는 '전체 경보 위치당 실제 적용된 고유 출발창' 비율이며 개별 경보행의 실행 가능률과 같지 않다. 미적용13개 고유창의 생산량 조건을 포함해 원시 사유를 shift_actions_v2에 보존했다. 준비60분은 모든 경보가 source_before_preparation이다.
- 새 피크0은 도착 네 구간 모두 이동 후 tau 이하라는 선택 제약을 검증한 결과이며, 제약 밖 운영에서 재피크가 없음을 입증한 수치가 아니다. 기본 D의 미적용13개 고유창(22경보행)은 invalid_observed_source_production으로 기록되었고 목적지 요금조건 실패가 아니다.

## 2026-09-24 검증 중 안전 경계 보강

- 선택A 독립 검토에서 진단의 OOF 선검사가 pandas usecols로 시각 열만 읽더라도 행의 다른 필드를 CSV 파서가 토큰화할 수 있음을 지적했다. 기존 OOF는 이미 개발 시각만임을 검증했고 실제 테스트 목표를 읽은 사건은 없었다. 방어를 통일하기 위해 공통 byte 단위 시각 선검사 read_development_oof를 진단에도 적용했다.
- 금지 경계 목표 뒤의 비정상 UTF-8 본문을 디코딩하기 전에 거부하는 합성 테스트를 포함해 안전 로더·진단11개 테스트가 통과했다. 예측/분할/선정 코드는 그대로이며 최종 cold 재현에서 진단 수치 불변을 다시 확인한다.

## 2026-09-24 자동 동결 예약 보류

- 읽기 점검에서 이전 지시로 등록된 FactoryPeakguard-Task05-Freeze-20261001이 Ready였고 현재 checkout의 scripts/run_frozen.ps1을 실행하도록 되어 있었다. 최신 지시는 테스트 접근을 금지하고 10/1 동결 승인을 미완료로 두므로, 이 특정 예약의 작업 경로를 검증한 뒤 Disable-ScheduledTask로 비활성화했다(확인 상태 Disabled).
- 원본 scheduled_freeze.json은 과거 예약 기록으로 보존하고 현재 상태는 session_0924_freeze_schedule.json에 별도 기록했다. 모델/과학 설정·평가 프로토콜·사전 기준은 바꾸지 않았다. configs/default.yaml의 이전 human_approval_required:false 문구는 이번 승인으로 간주할 수 없다. 사람의 명시적 동결 승인 및 최신 무결성 검토 전에는 수동 final 실행이나 예약 재활성화도 하지 않는다.
- 이 조치는 테스트 평가를 실행한 것이 아니라 기존 자동 실행을 막는 가역적 예약 변경이다. 사람 승인을 완료했다고 기록하지 않는다.

## 2026-09-24 회귀 검증 중 발견·수정

- 첫 `python -m pytest -q`는 이전 `_validation/package_reproduction/tests`까지 자동 수집하여 동일 모듈 이름9개의 collection mismatch로 중단됐다. 과학 계산 실패가 아니라 테스트 탐색 범위 충돌이었다.
- 기존 재현 패키지는 보존하고 pytest.ini의 testpaths=tests로 실제 프로젝트 테스트만 수집하게 했다. 재실행은85개 전부 통과(8.22초). 선택 작업의 후속 코드 통합 뒤 최종 전체 테스트를 다시 실행한다.

## 2026-09-24 선택 A — 피크 정의 민감도

- 사전 등록한 .90/.975와 참고 .95에서 원 선정 점모델·3계열 기준선 대표만 고정 설정으로 개발 CV를 재실행했다. 36개 거리·폴드·분위수 조합의 fit/stop/cal/score 경계·행 수가 유지됐다. .95 후보×폴드39개를 실제 재계산해 원 OOF와 최대5.68e-14 차이, 경보 완전 일치를 확인했다. 새 임계값의 피크 이력 특징과 가중치·보정 경보도 다시 계산했다.
- 참고 순위 .90에서는 h1/h4가 c2a_max_4_5_adjusted로 바뀌었고 h16/h96는 c3_holiday_hybrid를 유지했다. .975는 전 거리 원 순위와 같았다. 원 development_selection은 변경하지 않았다.
- h4 원 선정 모델의 피크 MAE는 .90에서14.041843 [11.679742,16.530141], .95에서14.443098 [12.347565,17.168300], .975에서20.053709 [15.967061,24.037682]이고 F1은0.628931/0.588589/0.601036이다. 다른 tau의 수치를 동일 사건집합 성능 개선으로 해석하지 않는다.
- .90의 h4에서 LightGBM 대비 CBL로 바꿀 때 피크 MAE 개선은−1.956 [−6.359,1.722]로 개선 미입증이다. F1·오경보 CI도 겹쳐 단순성 규칙으로 CBL이 참고 winner가 됐다. h1도 CBL 점추정이 더 나쁘지만 CI 중첩 후 동일 복잡도 이름순 동률 처리 결과가 바뀐 것이다. 'CBL이 더 우수' 또는 원 모델 교체로 해석하지 않는다.
- 모든 A CSV는 reference_only=True/selection_used=False이다. 순위 CI는 기존 선정의 날짜별 에피소드 분할 방식을 보존하고 한 날짜에 복수 폴드가 없음을 검사했다. 작업2의 신규 사건 시작일 클러스터 CI와 혼동하지 않는다. 전용5개 및 진단6개 테스트 통과, 단일 실제 실행30분 이내.

## 2026-09-24 선택 B — 관리 목표·준비시간 민감도

- h4/h16, 확인1/1·2/2·2/3, 준비15/30/60분의18조합을 생성했다. 관리 목표 T만 학습 .975 분위수(폴드182/182/181)로 바꾸고 사건 tau=.95와 기존 p_exceed를 그대로 유지했다. 각 폴드 fit_end/T/tau를 로그에 기록했다. 선정에 미사용이다.
- 1/1에서 h4는 경보999위치, 위치FP594, 에피소드F1=0.569149; h16은941위치/FP564/F1=0.516129다. 준비시간에 따라 경보 자체는 바뀌지 않고 조치 가능 시간 판정만 달라진다.
- 기존 구간 종료 시각 기준은 h4의60분 준비도 위치상100%로 계산한다. 실제15분 구간 시작 기준은 이 조합에서0%이다. 따라서 이를 실제 부하이동 가능률이라고 부르지 않는다. 에피소드 첫 경보가 실제 사건보다 앞선 경우의 선행시간은 별도 두 열(target_end_convention/interval_start)로 구분했다. 고유 1시간 출발창을 검사하는 작업4 비율과도 분모·시간 기준이 다르다.
- 독립 검토의 잘못된 at_start 열명을 실행 전에 수정했고 합성 사례로 종료1/시작0 판정을 검증했다. 실제 실행31.7초, 기존 선정·실제 피크 정의 불변이다.

## 2026-09-24 선택 C — 저녁 미탐과 생산량 가정 모델

- 기존 선정 점모델에 선정 분위수B의 위험 경보를 결합해 저녁 사건을 다시 매칭했다. FN7/TP11이 재현됐으며 18건 모두 공통 점예측 평가행에 포함됐다. 관측 생산량 직전4시간 변화 중앙값은 FN+1016/TP−256, 직전4시간 평균기온의 집단 평균은 FN28.182/TP27.375이다. 요일 분포는 별도 표에 기록하며 소표본의 사후 조건 차이를 인과 원인으로 해석하지 않는다.
- 원 h4 설정·3폴드·가중치2를 고정하고 미래 목표 시각 실적 생산량1열만 추가한 참고 모델과 원 점모델을 같은 행에 새로 학습했다. 학습/조기종료/보정 행은 원래와 같고, score 경계2행의 시간당 생산량이 미완료라 양쪽에서 함께 제외했다. 제외2행 모두 실제 피크이므로 피크425→423이며 공통 baseline 피크MAE14.141074를 원7551행의14.443098과 혼용하지 않는다. fresh baseline은 원 공통 OOF와 최대5.68e-14, 경보 완전 일치했다.
- baseline−oracle MAE 개선은 전체−0.292347 [−0.589069,−0.048244], 전체 피크−2.289554 [−3.522580,−0.803032], 저녁 전체−0.258519 [−0.590199,0.044627], 저녁 피크−4.413347 [−6.152902,−2.643601]이다. 저녁 전체 개선은 CI가0을 포함해 **개선 미입증**이며, 나머지는 이 고정 모델의 개발 OOF에서 악화 방향 CI이다.
- 원 위험 FN7건 중 참고 oracle 점경보 TP가2건(7/12 16:45,7/26 19:30)이다. 원 위험 경보의 FN을 TP로 바꾼 성능 개선으로 부르지 않는다. 기존 위험 사건과 참고 점경보의 종류·분모를 분리했다.
- 모든 oracle 결과에 **사후 관측 생산량 기반 사후 가정 시나리오**, reference_only=True, selection_used=False를 명시했다. 완벽한 미래 실적 가용성은 실제 생산계획의 가용/정확성을 검증한 것이 아니며 모델 성능의 수학적 상한도 아니다. 이 결과는 생산계획 정보 추가만으로 개선된다는 근거를 제공하지 않지만 생산량이 일반적으로 무관하다는 증명도 아니다.
- 실제 실행 약70초, 관련17개 테스트와6개 표·예측 해시 검증 통과. 숫자 계산은 유지한 채 시나리오 표기와 공통행 제외 사유 메타데이터를 보강했고 원 선정은 변경하지 않았다.

## 2026-09-24 cold 재현 사전 검사 실패와 복구

- 첫 cold 실행은 보호 해시 검사에서 재학습 전에 중단됐다. 최초의 과도한 pytest 수집(11:24:03)이 prototype 아래에 새로 만든 __init__/forecast/test_forecast의 Python 캐시3개가 원래75개 목록에 추가된 탓이었다. 원본75개 파일의 내용 변경은 없었고 사전 목록에는 pycache가 없음을 확인했다.
- 캐시3개 삭제 명령은 자동 승인 검사에서 blocked by policy로 실행 거부됐다. 더 가역적인 조치로, 정확한 workspace 경계를 검사한 뒤 해당3파일만 `_validation/session_0924_generated_cache/`로 Move-Item하여 보존했다. 다른 원본 파일은 옮기거나 삭제하지 않았다.
- 보호 검사 재확인 결과75개/unchanged=True. pytest.ini로 탐색을 tests/에 한정했으므로 보호 디렉터리의 테스트를 다시 수집하지 않는다. 재현 결과 수치 확인 전의 실패이며 새로 빈 폴더에서 cold 검증을 다시 시작한다.

## 2026-09-24 task 6 — 최종 회귀·부분 재현 완료

- 선택 A/B/C 통합 후 전체 테스트93개가8.23초에 통과했다(직접 Python 종료코드0). 누수·보조 지표·지연 이동 시간 제약·share 경계·안전 입력 검사·재현 검증기 회귀를 포함한다.
- 빈 `_validation/session_0924_repro`에서 작업1~5를 새로 계산해516.04초에 통과했다(직접 Python 종료코드0). 21개 CSV의 수치 최대 절대차0이며, 기존 CV252행의 기존 지표 최대차5.68e-14 및 피크 MAE CI 최대차1.42e-14로 허용오차1e-10 안이다.
- 원본 OOF·선정 JSON·보호75개 파일은 전후 해시/목록이 일치했다. h1=p1_latest, h4=lgbm_no_holiday_weight_2, h16/h96=c3_holiday_hybrid를 유지한다. addendum·원 채택 기준·평가 프로토콜·훈련 코드는 바꾸지 않았다.
- 부분 재현은 보존된 개발 OOF·원 모델·입력 및 비교표를 사용한 검증이며 전체 후보 파이프라인의 빈 환경 실행이 아니다. 선택 A/B/C는 이번 두 번째 cold 실행에 포함하지 않았고 실제 실험 및 별도 회귀/원본 예측 일치 확인 범위를 인계서에 명시했다.
- 최종 테스트는 열지 않았고 final_test.csv는 없다. 사람 입력은 전부 미완료로 남기며 기존 자동 동결 예약은 명시적 승인 전까지 Disabled이다. 상세 증거는 outputs/logs/session_0924_repro.json 및 session_0924_summary.md에 기록했다.

## 2026-09-24 P1 기반 안전 결정

- 최신 마스터 지시가 이전 문서 갱신 요청보다 우선한다. report/slides/submission/roadmap/verification/prototype는 보존하며 테스트 목표는 2021-08-09 09:45 이전만 허용한다. P1 확증23개 가설은 2138770에서 결과 전에 고정했다.
- 원래 run_all 개발 단계는 전체 CSV를 읽은 뒤 자르므로 현재의 비접근 조건에 맞지 않았다. 기본 명령과 개발 전용 명령을 metadata-first prefix 입력으로 전환했다. 다른 전체 데이터 단계는 날짜 및 이름/시각/현재 해시에 결합된 사람 승인 검사 뒤에만 진입한다. 설정의 과거 human_approval_required:false는 승인으로 인정하지 않는다.
- dry-run은 원자료의 불투명 해시, 개발 캐시/설정/선정/코드 지문만 검사하고 테스트를 파싱하지 않는다. 새 과학 모듈이 캐시를 무효화하도록 fingerprint 범위를 확장했다. 실제 개발 재학습과 재봉인은 P3 사전 고정 후에만 수행한다.
- 자동 실행 기록이 PROGRESS의 이전 marker 뒤 수동 기록을 삭제하던 동작은 append-only로 고쳤다. 같은 Phase의 누락/표본 부족 가설도 Holm 가족에서 제외하지 않고 p=1로 유지한다.

## 2026-09-24 P1 결과와 게이트

- 사전 정의를 넓히지 않았다. A1 효과+0.006851, CI[-0.012364,0.029778], Holm p=1로 미입증이다. 학습 선정 슬롯은0/0/1개이며 선택 슬롯의 피크0건으로 A2 16개와 A3 4개 가설은 평가 불가 p=1로 유지했다. 일반적 피크 예측 불가능성을 입증한 것이 아니다. feature_spec_P2의 추가 특징은 없으며 M2/M3를 건너뛴다.
- A4 오류 집중 효과는 h16 -11.301[-32.663,8.245], h96 -16.267[-37.163,2.319]로 미입증이다. h96 1개 비정의 재표집은 검정에서 보수적으로 비기각에 포함했고 유효999개로 CI를 산출했다.
- A5 동일 규칙의 h16 준비30/60분 실제 피크 출발창 조치는172/183이며 완벽정보 참고178/183이다. h4는 원 경보166개보다 완벽정보의115개가 적다. 목표 피크 위치에서만 발령하는 oracle가 같은 시간의 더 이른 비피크 경보를 없애기 때문이다. 이를 조치 수의 수학적 상한으로 주장하지 않는다.
- 구현 검토에서 pandas datetime64[us]의 asi8을 ns로 간주한 검출 오류를 발견해 단위를 정규화하고 us/ns 동일성 테스트를 추가했다. 주차 슬롯률의 평일 분모와 빈 결과 CSV 스키마를 수정했다. 결과에 따른 임계값/코호트/후보 변경은 없었다.
- 독립 검토 및 부모14개 집중 테스트를 통과했고 표와 과학 그림을 확인했다. forking_paths에는 확증23가설(추정 가능3), 등록16개 로지스틱 구성(적격 학습0), 재가동5정의와 운영16시나리오를 따로 기록한다.

## 2026-09-24 P2 M1 잔차 후보

- 사전 고정3889a8e의 CBL 잔차 후보만 h4/h16/h96 각3폴드, 총9회 적합했다. 원본 분할·tau·선택 하이퍼파라미터를 그대로 재구성했고, 36개 분할에서 기준선 결측 제외0행이다. 후보의 b와 원 CBL OOF의 최대 차이는0이다.
- 기존 선정 대비 피크 MAE 개선량은 h4 -5.309 CI[-7.753,-2.352], h16 -4.492 CI[-12.073,0.407], h96 -6.461 CI[-11.004,-2.652]다. h4는 참고 전용, h96은 마지막 폴드 MAE1.504배로 배제한다. h16 마지막 폴드 비율0.908은 안정성 조건을 통과하지만 피크 MAE 개선은 미입증이다.
- 같은 원점·목표·폴드의 대응 표본에 기존 선정 순서를 적용하자 h16은 피크/F1 CI가 겹친 뒤 위치 오경보 CI 감소로 선택됐다. 이는 피크 MAE가 개선됐다는 결론이 아니며, q95 경보 모델 교체도 아니다. 실제 채택/비채택은 별도 P2_candidate_decisions 표에 기록한다.
- 독립 코드 검토에서 새 누수·분할 오류를 찾지 못했다. 원점 이후 관측 교란, 잔차 복원, 기준선 결측 비보간, 원본 fold parity 테스트를 통과했다.

## 2026-09-24 P2 M4 적응 q95 후보

- h16/h96 각 rolling672·ACI gamma .005/.01/.05, 총8후보를 평가했다. 모든 후보는 최대672개 완료 잔차 버퍼를 공유하며 ACI만 beta를 갱신한다. 사전 등록의 이 공통 창 정의를 결과 후 전체 누적 잔차로 바꾸지 않았다. 임시 코드 제안은 실행 전에 철회되어 새 후보나 추가 결과가 없다.
- 원 분위수/B 보정 OOF는1e-10 이내로 재현됐고, target_time<=origin인 실측만 업데이트에 반영한다. ACI는 당시 발행 q95의 hit를 사용한다. 초기 보정 목표의 봉인 경계, 목표 시각 증가, 단일 예측거리 검사도 추가했다.
- h16 네 후보는 상위 q95 커버리지 오차가4.13~5.43%p 악화됐다. h96 rolling은 명목 개선+3.790%p CI[1.100,6.423]%p이나 Holm27 p=.1329로 다중검정 후 미입증이고, 마지막 폴드 커버리지1.688%p 하락으로 미채택이다. 다른 h96 후보도 마지막 폴드 조건을 통과하지 못했다. 8후보 모두 B를 대체하지 않는다.
- P2 전체27가설 중 Holm 통과8개는 모두 pinball의5% 허용폭 조건이며 커버리지 개선 입증8개가 아니다. 양의 개선 가설에 대한 반대 방향 명목 CI는 표에 기각으로 표시하되 음의 효과의 다중검정 유의성을 별도로 주장하지 않는다.
- M4는 동일 고정 설정으로2회 실행해 분위수 모델 실제60회 적합했다(첫 실행 후 폴드별 효과 기록 검증 목적). 고유 후보는8개 그대로다. 독립 검토 및16개 관련 테스트를 통과했다. 향후 적응 보정을 최종 평가에 넣으려면 별도 채택·최종 통합이 필요하며 현재 dry-run은 미지원 적응 선정값을 거절한다.

## 2026-09-24 P2 통합 결정

- 기존 `_selection`을 고치지 않고 먼저 실행한 뒤 원 선정과 연구 challenger를 비교하는 선택 확장만 연결했다. 원 선정은 manifest.original_selection에 남기고 h4 잔차는 참고 전용으로 유지한다. 기본 YAML에 고정 연구 후보 경로를 활성화하고 최신 지시에 맞춰 사람 동결 승인 필요를 명시했다.
- h16 선택은 CBL c3→잔차 모델이며 FP978→493의 CI 분리로 결정됐다. 평균 피크 MAE15.285→19.778 및 첫 폴드23.760→68.570의 악화는 함께 공개한다. 마지막 폴드만10% 악화를 제한하는 사전 기준을 사후에 강화하거나 완화하지 않는다.
- 선택된 점·위험 조합으로 A5를 다시 계산했다. q95 위험 출력이 그대로이므로 D20% 준비30/60분의 실제 피크 출발창172/183, 완벽정보178/183이 동일했다. 예측 개선으로 조치율이 상승했다고 주장하지 않는다.
- 채택 잔차가 최종 승인 후에도 y-b로 학습하고 b+f로 복원되도록 최종 준비 코드를 연결했다. baseline NaN은 보간하지 않으며 미래 평가에서는 유효 평가행 수를 명시해야 한다. 실제 final 경로는 호출하지 않았고 합성 테스트로만 검사했다.
- 캐시 지문에 사전등록 문서·P1 특징 게이트와 적응 보정 번들을 포함해 수정 시 무효화한다. 독립 통합 검토, 신규후보80,816행 계약 및22개 집중 테스트를 통과했다. 최종 재봉인은 P3에 한 번 수행한다.

## 2026-09-24 P3 검증 구현

- 0cae04d에서 결과 비교 허용오차를 먼저 고정했다. 독립 P2 CSV 재읽기와 P3 메모리 내 계산 사이의 부동소수점 직렬화 차이는 abs1e-10로 허용하되, 모델명·채택 사유·키·bool·결측 위치는 정확히 같아야 한다. 비교 열 누락도 실패로 처리한다.
- 캐시 없이 A1/A2/A4를 재계산한 주요 통계와 표는 최대 절대차0이다. 원본 OOF/manifest와 P1 산출물40파일의 전후 해시를 확인했다. 별도 P1 재현 결과를 통합 검증기에 연결해 불필요한 중복 계산을 피한다.
- 전체 pytest140개가15.57초에 통과했다. 실제 Windows 동결 예약은 Disabled이며, 과거 Ready를 담고 있던 scheduled_freeze.json도 실측 상태로 갱신했다. 예약 활성화나 승인 파일 생성은 하지 않았다.


### P3 ?? ??? ?? ?? ??
- ?? ???? P2 ?? ??? ?? ?? ?? ??? ????. ?? CV ?? ? ??? 10? ??? ?? CV? h96 ?? ?, ?? ?? ??? ?? ??? ????. ?? ? ????? ?? ???? ???.
- ? ?? ??? ?? P2 ??? ??? ?? ????, M1? ?? ??? M1_parity ?? ??? ????. ????? ?? ? ???? ???? ????.


### P3 ??? ?? ??
- ????? ??? ???? ???? P1 ?? ?? ??? ??2?? ??? ??40? ???? ???? ??? ??? ????. ?? ??/?? ??/??? ??? ???.
- P1 ?? ?? ?? ??? ??6? ?? ??141? ??? ??? ????. ???? ? P2 manifest ?? ? ???? ??? ????.
