# 평가 프로토콜

2026-09-24 개발 실행 전 고정. 원자료와 verification/의 해시는 별도 manifest로 보존한다.

1. 사전 검증에서 마지막 15% 테스트를 고정 설정으로 이미 1회 열람했다. 기존 수치는 역사적 증거이며 신규 모델 성능이 아니다.
2. 기존 피처 필터 뒤 85% 경계의 첫 테스트 원점은 2021-08-09 09:45, T1 목표 시각은 10:45이다. 신규 피처로 행 수가 달라져도 이 경계를 옮기지 않는다.
3. 개발 3폴드는 확장 학습, 분리된 보정/임계 선택, 이어지는 점수 평가 구간을 가진다. 학습·보정 목표의 끝은 다음 구간 원점보다 엄격히 이전이다.
4. y(t)는 t 구간 종료 직후 가용하므로 관측 최대 시각 ≤ 원점이다. 달력은 사전 가용. 실측 기상·동시간 생산·인원은 사후 분석 전용.
5. 복원 행뿐 아니라 복원 관측을 사용한 모든 피처와 목표 행은 제외한다. CBL도 참고일 < 목표일, 참고 관측 ≤ 원점이다.
6. 피크 τ는 학습 관측만의 95백분위. 각 폴드 재산정. 경보 임계는 별도 보정 구간의 에피소드 F1 최대.
7. 모델 간 비교는 동일 유효 행, 날짜 블록 1000회, 쌍별 재표집. 에피소드는 실제 시간 간격이 15분보다 크면 끊는다. 일대일 겹침 매칭.
8. 최종 테스트는 2026-10-01 18:00 KST 이후 모델·코드·설정 동결 manifest를 만든 뒤 한 번만 실행한다. 이전 실행은 개발 결과만 생성하며 final_test.csv를 만들지 않는다.
9. 재실행은 검증된 캐시/동결 결과를 재사용한다. 이미 완료한 테스트의 재학습/재평가·덮어쓰기는 거부한다. 같은 고정 모델의 결과 재현과 새로운 테스트 탐색은 구분한다.
10. 최종 성공은 사전 목표이며 보장하지 않는다. CBL·persistence 대비 CI, 상위 q95 커버리지 부족, 저녁 FN을 실패 결과까지 그대로 보고한다.
