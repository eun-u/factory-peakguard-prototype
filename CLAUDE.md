# 마스터 프롬프트: 제6회 K-인공지능 제조데이터 분석 경진대회 과제 ⑤

이 문서는 프로젝트 전 기간 동안 에이전트가 매 세션 시작 시 읽는 기준 문서다.
프로젝트 루트에 `CLAUDE.md`로 저장해 사용한다. 작업 중 이 문서와 충돌하는 판단이 필요하면
임의로 진행하지 말고 사람에게 확인을 요청하라.

---

## 1. 프로젝트 개요

### 1.1 대회
- 대회: 제6회 K-인공지능 제조데이터 분석 경진대회 (일반국민/대학(원)생 부문)
- 과제: **⑤ 제조 생산데이터 기반 전력사용량 예측 및 최대피크 위험조건 분석** (자원 최적화 AI 데이터셋)
- 출제 요구
  1. 향후 지정된 시간구간의 전력사용량을 예측하는 AI 모델 개발
  2. 예측오차가 크게 발생하는 생산조건 분석
  3. 최대전력 피크가 발생하는 주요 조건 분석
  4. 생산일정 또는 설비가동 시점 조정을 통한 피크전력 저감방안 제안
- 제출 마감: **2026-10-08(목) 23:59**. 내부 목표 제출일: **2026-10-07(수)**
- 이후 일정: 서면평가 결과 10-26(월), 발표평가 10-30(금, 팀원 전원 참석 원칙)

### 1.2 평가표 (서면평가, 100점)
| 장 | 항목 | 배점 | 요구 내용 |
|---|---|---|---|
| 1 | 데이터 이해 및 진단 | 15 | 생산단위, 변수 의미, 시간·설비·제품 관계, 결측·중복·이상치·불균형 분석, 전처리 및 검증전략 |
| 2 | AI 예측모델 개발 | 40 | 베이스라인 포함 2개 이상 모델 비교(F1 등), 최종모델 선정근거 |
| 3 | 영향요인 및 오류분석 | 15 | 주요 영향변수와 상호작용, FN·FP가 집중되는 조건 |
| 4 | 현장 활용방안 | 10 | 사전경보, 점검, 의사결정 활용 방안 (⑤는 피크전력 저감방안) |
| 5 | 창의성·차별성 | 10 | 불확실성 추정, 확률보정, 제조지식 결합 등. **2·3장 결과에 대한 기여로 설명** |
| 6 | 코드 및 재현성 | 10 | 전처리부터 학습·추론·결과생성까지 자동 실행 |

동점 시 정성지표 총합 > 정량지표 순. 정량지표의 정체는 확인되지 않았다(가정 A2 참고).

### 1.3 제출물
1. 결과 보고서 PDF (지정 hwpx 양식, 본문 휴먼명조 14 / 줄간격 160, 세부 휴먼명조 10)
2. 소스코드 zip: 소스코드 + requirements.txt, 학습용 데이터, README, 테스트데이터 예측결과 파일
3. 발표자료 PDF와 PPT 둘 다
4. 만족도 설문 완료 화면 캡처 (보고서에 첨부 필수)

**블라인드 규칙**: 모든 제출물에 학교명, 기업명, 로고 등 소속 식별 정보 금지. 성명과 팀명만 허용.
코드 주석, 파일 경로, 그림 제목, PPT 메타데이터(작성자·회사 필드)까지 해당한다.

---

## 2. 이미 확정된 결정 (변경 시 사람 승인 필요)

과제 선택 근거는 `verification/` 아래 사전 검증 결과에 있다. 이 폴더는 **증거 보존용으로 동결**하며 수정하지 않는다.

- ② 로봇용접은 개별 용접 불량 라벨이 없어 제외, ③ 프레스 이상탐지는 대안으로만 유지
- ⑤ 사전 검증 핵심 결과 (1시간 후 15분 예측, 마지막 15% 테스트)
  - 최선 기준선: 직전 15분 관측값(persistence). 계절 나이브는 약한 기준선이었음
  - 피크 위치 MAE: persistence 27.73 → LightGBM 17.77, 개선률 35.9% [19.6, 50.1]
  - 피크 위치 적중/오경보: persistence 289/774 → LightGBM 295/305
  - 위치 F1 0.403 → 0.607, 에피소드 F1 0.451 → 0.636
  - 약점: 16~24시 피크 재현율 0.143, 95% 분위수 실제 커버리지 0.911, 익일 최대 예측 MAPE 악화
  - 데이터 한계: 12월 없음·9월 중순까지(완전한 하계·동계 창 없음), 전력 단위·15분 경계 미확인,
    시간 오류 48행, 설비 ID·제품·실제 교대 정보 없음

---

## 3. 주최측 답변 없이 진행하기 위한 작업 가정

주최측 답변이 오면 `DECISIONS.md`에 기록하고 해당 가정만 교체한다. 파이프라인 구조는 바꾸지 않는다.

| ID | 가정 | 근거와 대응 |
|---|---|---|
| A1 | 주 예측 과제는 **1시간 후 15분 전력**, 보조 과제는 **익일 일간 최대 15분 전력** | 출제문의 "지정된 시간구간" 미확정. 예측거리 곡선(15분·1시간·4시간·24시간)으로 모든 해석을 덮는다. 대회 데이터에 테스트 파일이나 제출 양식이 있으면 그 형식이 A1보다 우선한다 |
| A2 | 정량지표는 2장 모델 성능일 가능성이 가장 높다 | 수치 성능과 비교 설계의 엄밀성을 모두 확보한다. 주 지표는 4.3절에 고정 |
| A3 | 전력 단위는 미확인으로 표기 | "kW", "kWh"로 단정하지 않는다. 비용 분석은 비율과 민감도로만 제시 |
| A4 | 15/30/45/60분 열은 HH:15, HH:30, HH:45, (HH+1):00에 끝나는 연속 15분 구간 | 보고서 가정 목록에 명시 |
| A5 | 피크 = 학습 구간 전력 상위 5% 초과 | 계약전력 초과가 아닌 통계적 사건. 민감도로 상위 2.5%·10%도 보고 |
| A6 | 설비 동시 가동·제품 전환·교대는 대리변수로 분석 | 시간대(00–08, 08–16, 16–24), 요일, 생산량 구간, 월. "대리변수"임을 매번 명시 |
| A7 | 테스트데이터 예측결과 파일 | 주최측 테스트 데이터가 있으면 그것을 예측. 없으면 마지막 15% 테스트 구간 예측을 A1 두 과제 각각 CSV로 제출 |
| A8 | 생산량은 해당 시간이 끝난 뒤에만 안다 | 미래 생산량, 실측 기상, 같은 시간 평균 전력은 입력에서 제외 |

---

## 4. 작업 규칙

### 4.1 누수 방지
- 모든 분할은 시간순. 랜덤 분할 금지
- 각 예측 시점에서 실제로 관측 가능한 값만 특징으로 사용. 특징마다 "가용 시점"을 `features.py` 주석에 기록
- 학습·검증·테스트 경계에는 예측거리만큼 간격을 둔다
- 피크 임계값, 스케일러, 보정기 등 모든 통계량은 학습(또는 학습+검증) 구간에서만 계산

### 4.2 테스트 구간 사용 규칙
사전 검증에서 마지막 15% 테스트 구간을 이미 한 번 봤다. 단, 설정 튜닝 없이 고정 설정으로만 봤다.
이 사실을 보고서 1장 검증전략에 그대로 적는다. 이후에는:
- **모델 개발과 튜닝은 앞 85% 안의 rolling-origin 교차검증으로만** 한다
- 테스트 구간은 모델 동결(10-01) 이후 **최종 평가 1회**만 사용한다
- 동결 이후 테스트 결과를 보고 모델을 바꾸면 안 된다. 바꿔야 할 이유가 생기면 사람에게 보고하고 결정을 `DECISIONS.md`에 남긴다

### 4.3 평가 프로토콜 (Phase 2 시작 전에 `eval_protocol.md`로 동결)
- 개발 평가: 앞 85%에서 rolling-origin 3폴드, 각 폴드마다 피크 임계값을 해당 학습 구간에서 재계산
- 최종 평가: 마지막 15% 테스트 1회
- 신뢰구간: 날짜 블록 부트스트랩 1000회, 95% 백분위
- 주 지표 (이 순서로 선정근거에 사용)
  1. 피크 위치 MAE
  2. 피크 에피소드 F1 (연속 15분 초과 구간을 1사건으로, 경보 에피소드는 겹치는 실제 에피소드 최대 1개와 매칭)
  3. 피크 오경보 수 (15분 위치 기준)
  4. 분위수 모델의 커버리지 오차 |실제 커버리지 − 명목| (0.9, 0.95)
- 보조 지표: 전체 MAE, RMSE, MAPE(0 제외), 평균 pinball loss, Brier score, PR-AUC, 날짜 단위 F1(느슨한 지표임을 명시)
- 모든 비교는 같은 분할, 같은 임계값 결정 규칙(검증 구간 F1 최대)에서 수행

### 4.4 코드 규칙
- Python + pandas, numpy, scikit-learn, lightgbm, matplotlib, pyyaml. 딥러닝은 사용하지 않는다
- seed 42 고정. 설정값은 `configs/default.yaml`에서만 읽는다
- 모든 그림과 표는 스크립트로 생성해 `outputs/`에 저장. 손으로 만든 결과물 금지
- 그림 텍스트는 한국어, 한글 폰트 설정을 `viz.py`에 한 번만 정의
- `python run_all.py` 한 줄로 전처리 → 학습 → 평가 → 분석 → 예측 파일 → 그림·표가 모두 재생성되어야 한다
- 소속 식별 정보를 코드·주석·경로에 쓰지 않는다

### 4.5 기록 규칙
- `PROGRESS.md`: 세션 종료 시 날짜, 한 일, 새로 나온 핵심 수치(CI 포함), 막힌 점, 다음 할 일
- `DECISIONS.md`: 결정 날짜, 결정 내용, 이유, 근거 파일. 가정 변경도 여기에 기록
- 부정적 결과도 기록한다. 좋은 결과만 남기지 않는다

### 4.6 세션 시작·종료 루틴
- 시작: 이 문서 → `PROGRESS.md` 마지막 항목 → `DECISIONS.md` 순서로 읽고, 오늘 작업이 로드맵 몇 단계인지 한 줄로 밝힌다
- 종료: `run_all.py`가 오류 없이 도는지 확인하고 `PROGRESS.md`를 갱신한다

---

## 5. 저장소 구조

```
project/
├─ CLAUDE.md                 # 이 문서
├─ PROGRESS.md
├─ DECISIONS.md
├─ eval_protocol.md          # Phase 2 전 동결
├─ README.md
├─ requirements.txt
├─ run_all.py
├─ configs/default.yaml
├─ data/raw/                 # 원자료, 읽기 전용
├─ data/processed/
├─ src/
│  ├─ data.py                # 로딩, 시간 오류 복원, 15분 전개, 품질 점검
│  ├─ features.py            # 달력, 시차, 이동통계, 완료 생산량 (가용 시점 주석 필수)
│  ├─ split.py               # 시간순 분할, rolling-origin, 경계 간격
│  ├─ models/
│  │  ├─ baselines.py        # persistence 3종, 계절 나이브 3종
│  │  ├─ lgbm_point.py
│  │  ├─ lgbm_quantile.py    # 분위수 + conformal 보정
│  │  └─ peak_classifier.py  # 피크 초과 확률 직접 분류
│  ├─ evaluate.py            # 4.3절 지표, 에피소드 매칭, 부트스트랩
│  ├─ analysis/
│  │  ├─ horizon_curve.py    # 예측거리별 성능
│  │  ├─ errors.py           # 조건별 오차, FN·FP 집계
│  │  ├─ peak_conditions.py  # 피크 발생 조건
│  │  ├─ importance.py       # permutation importance, PDP
│  │  ├─ alerting.py         # 경보 규칙, 오경보 KPI
│  │  ├─ decision.py         # cost-loss, REV
│  │  └─ simulate_shift.py   # 생산량 시간대 이동 시뮬레이션
│  └─ viz.py
├─ outputs/{figures,tables,predictions,logs}/
├─ report/                   # 장별 초안 md
├─ slides/                   # 발표 구성안과 그림
├─ submission/               # 최종 제출 패키지
└─ verification/             # 사전 검증 증거, 동결
```

---

## 6. 로드맵

추석 연휴(9월 말)와 개천절(10-03)이 일정 안에 있다. 연휴에는 작업량이 줄어들 수 있으므로
각 단계의 "완료 기준"을 넘으면 즉시 다음 단계로 넘어가고, 일정이 밀리면 7절의 삭감 순서를 따른다.

### Phase 0. 기반 구축 — 9/23(수)~9/24(목)
**목표**: 현재 검증 결과를 새 구조에서 재현하는 뼈대
- 대회 데이터 폴더 전체를 다시 훑어 테스트 파일, 제출 양식, 데이터 설명 문서가 있는지 확인.
  있으면 A1·A7을 갱신하고 `DECISIONS.md`에 기록
- `verification/t5_power.py`, `t5b_persistence.py`의 로직을 5절 구조로 옮긴다 (동작 변경 없이)
- `configs/default.yaml`, `run_all.py`, `requirements.txt`, 빈 `PROGRESS.md`·`DECISIONS.md` 생성
- (사람) 주최측 문의 발송, 만족도 설문 완료 후 캡처 보관

**완료 기준**: `python run_all.py`가 사전 검증의 persistence·LightGBM 피크 위치 MAE를 소수 둘째 자리까지 재현

### Phase 1. 데이터 이해·진단 → 보고서 1장 — 9/25(금)~9/27(일)
- 데이터 사전: 변수별 의미, 단위(미확인 포함), 측정 단위(시간 행 × 15분 4열), 가용 시점
- 품질: 시간 오류 48행의 위치와 복원 방식, 0값 74개의 분포(연속 여부, 시간대, 요일 → 비가동 추정 근거), 결측, 중복, IQR 이상치
- 구조: 월·요일·시간대별 전력 분포, 전력과 생산량·기온·인원의 관계(분석용, 모델 입력 아님), 피크 사건의 불균형 비율
- 계절 범위 한계(12월 없음, 9월 중순까지)와 그 의미
- 검증 전략: 시간순 분할 이유, 경계 간격, rolling-origin, 테스트 1회 원칙, 사전 검증에서 테스트를 이미 본 사실
- 누수 방지 규칙 표 (A8 포함)

**산출물**: `report/ch1_data.md`, 그림 5~7개, 표 3~4개
**완료 기준**: 평가표 1장 요구 항목(생산단위, 변수 의미, 시간·설비·제품 관계, 결측·중복·이상치·불균형, 전처리, 검증전략) 각각에 대응하는 절이 있다

### Phase 2. 모델 개발 → 보고서 2장 — 9/27(일)~10/1(목)
**시작 전 필수**: `eval_protocol.md` 작성·동결

2-1. 베이스라인 (9/27)
- persistence 3종(직전 15분, 1시간 전 동일 위치, 최근 1시간 평균), 계절 나이브 3종(전일, 전주, 평균)
- 각 계열에서 개발 CV 전체 MAE 최소를 대표로 고정

2-2. LightGBM 점예측 개선 (9/28~9/29)
- 특징 확장 후보: 다중 시차(15분~1주), 이동 평균·최대·표준편차, 직전 피크 이후 경과시간, 요일×시간 상호작용, 완료 생산량의 시차
- 피크 가중: 학습 샘플 가중치 또는 피크 구간 가중 손실 비교
- 튜닝은 개발 CV에서만. 탐색 범위와 횟수를 `DECISIONS.md`에 기록

2-3. 분위수 예측과 보정 (9/29~9/30)
- LightGBM quantile(0.1, 0.5, 0.9, 0.95)
- 개발 구간 보정 세트로 split conformal 보정 → 0.9, 0.95 커버리지 목표 충족 확인
- 보정 전후 커버리지·pinball loss 비교

2-4. 피크 초과 확률 (9/30)
- 방식 A: 보정된 분위수에서 초과 확률 보간
- 방식 B: LightGBM 분류기 직접 학습 + 확률 보정(Platt 또는 isotonic)
- F1 임계값은 검증 구간에서 결정. Brier, 신뢰도 곡선 비교

2-5. 예측거리 곡선 (9/30~10/1)
- 예측거리 15분, 1시간, 4시간, 24시간 각각 persistence·계절 나이브·LightGBM 비교
- 익일 일간 최대 15분 예측(A1 보조 과제) 별도 표
- "어느 예측거리까지 모델이 기준선을 의미 있게 이기는가"를 명시

2-6. 최종 모델 선정과 동결 (10/1 18:00)
- 4.3절 주 지표 순서로 선정근거 작성
- 테스트 최종 평가 1회 실행, 결과를 `outputs/tables/final_test.csv`로 저장
- 최종 모델, 설정, 해시를 `DECISIONS.md`에 기록

**산출물**: `report/ch2_model.md`, 모델 비교표(CI 포함), 예측거리 곡선, 신뢰도 곡선, 적중/오경보 비교 그림
**완료 기준**: 베이스라인 포함 4개 이상 모델이 동일 조건에서 비교되어 있고, 선정근거가 지표 순서대로 서술되어 있다

### Phase 3. 영향요인·오류분석 → 보고서 3장 — 10/1(목)~10/3(토)
- 영향변수: permutation importance(테스트), 상위 변수 PDP·ICE, 주요 상호작용 1~2개
- 오차 집중 조건: 시간대(대리 교대), 요일, 월, 생산량 구간별 MAE와 피크 FN·FP (날짜 블록 CI)
- 핵심 실패 사례: 16~24시 피크 미탐의 원인 분해. 피크 직전 전력 추세, 생산량 변화, 요일 패턴을 비교하고 대표 사례 3개를 시계열 그림으로
- 피크 발생 주요 조건(출제 요구 3): 피크 에피소드의 시간대·요일·월·생산량 분포, 비피크 대비 차이
- 모든 조건 분석은 탐색적이며 다중 비교 조정이 없음을 명시. 대리변수 사용을 명시

**산출물**: `report/ch3_errors.md`, 그림 6~8개
**완료 기준**: "모델이 잘 되는 조건"과 "실패하는 조건"이 분리되어 있고, FN·FP 집중 조건이 표로 제시되어 있다

### Phase 4. 현장 활용·창의성 → 보고서 4·5장 — 10/3(토)~10/4(일)
4-1. 경보 운영 규칙 (경보관리 표준·SRE 알림 설계 차용)
- 피크 초과 확률 기반 2단계 경보(주의/조치), 연속 확인 규칙(1/1, 2/2, 2/3) 비교
- 규칙별 운전시간당 오경보, 적중률, 경보 선행시간

4-2. 조치 임계값 (기상 cost-loss 모델 차용)
- C/L 비율 스윕, 보정 후 REV 곡선 재측정, 기후평균 대비 가치
- 한전 15분 최대수요와 12개월 기본요금 연동 구조를 손실 L의 근거로 설명. 금액은 단위 미확인이므로 비율로만

4-3. 피크 저감 시뮬레이션 (예측 오토스케일링 차용)
- 경보 발생 시 해당 시간대 생산량 일부(10·20·30%)를 비피크 시간대로 이동한다고 가정
- 생산량–전력 관계는 학습 구간에서 추정한 단순 회귀로 근사하고, 이동 후 월 최대 15분 전력 변화를 추정
- 인과 효과가 아닌 가정 기반 시뮬레이션임을 명시하고 민감도로 제시

4-4. 창의성 정리 (5장)
- 기법 나열 금지. 각 기법이 2·3장의 어떤 수치를 얼마나 바꿨는지로 서술:
  conformal 보정 → 커버리지 오차, 피크 가중·분류 → 오경보 수, 경보 규칙 → 운전시간당 오경보, cost-loss → REV
- 사전 판정 기준을 먼저 고정하고 검증한 절차 자체를 방법론적 차별점으로 1개 절 작성

**산출물**: `report/ch4_field.md`, `report/ch5_novelty.md`, 경보 운영 흐름도, REV 곡선, 시뮬레이션 그림
**완료 기준**: 모델 출력 → 경보 → 조치 → 기대 효과가 하나의 흐름으로 연결되어 있다

### Phase 5. 재현성·보고서 완성 — 10/4(일)~10/6(화)
- 새 가상환경에서 `pip install -r requirements.txt` 후 `python run_all.py` 전체 실행 테스트, 소요 시간 기록
- README: 환경, 설치, 실행 명령, 폴더 구조, 산출물 위치, 예상 실행 시간, 가정 목록
- 테스트데이터 예측결과 파일 생성(A7)
- `report/ch6_repro.md` 작성
- (사람) 장별 초안을 hwpx 양식으로 옮기고 서식 적용(휴먼명조 14/10, 줄간격 160), 작성요령 문구 삭제, 설문 캡처 첨부, PDF 변환

**완료 기준**: 다른 PC(또는 새 환경)에서 README만 보고 전체 재실행 성공

### Phase 6. 발표자료 — 10/6(화)~10/7(수)
- 슬라이드 구성안 `slides/outline.md` (12~15장 권장):
  문제 정의 → 데이터 진단과 한계 → 검증 전략 → 모델 비교 → 선정근거 → 예측거리 곡선 →
  오류분석(저녁 피크) → 피크 발생 조건 → 경보 운영 → 저감 시뮬레이션 → 차별점 → 한계와 향후 과제
- 그림은 `outputs/figures`에서 재사용
- PPT와 PDF 둘 다 생성. PPT 파일 속성의 작성자·회사 필드 비우기

### Phase 7. 제출 — 10/7(수)
- `submission/` 패키지 구성: 보고서 PDF, 소스코드 zip(코드, requirements, 학습용 데이터, README, 예측결과 파일), 발표 PDF·PPT
- 블라인드 점검: 전체 파일에서 학교명·기관명·로고·개인 계정 경로 검색
- zip을 새 폴더에 풀어 `run_all.py` 한 번 더 실행
- KAMP 포털 제출 후 완료 화면 캡처
- 10/8(목)은 문제 대응용 예비일. 새 작업을 시작하지 않는다

### 마감 이후
- 10/26 결과 발표 후 상위 8팀이면 10/27~10/29 발표 연습
- 예상 질문 준비: 계절 불완전, 전력 단위 미확인, 새 계절 일반화, 설비 정보 부재 시 저감방안의 실효성, 테스트 구간 재사용 여부

---

## 7. 일정이 밀릴 때 삭감 순서

아래 순서대로 줄인다. 위쪽 항목부터 포기한다.
1. 예측거리 4시간 (15분·1시간·24시간만 유지)
2. 피크 분류 방식 B (분위수 보간 방식 A만 유지)
3. 저감 시뮬레이션의 이동 비율 다양화 (20% 하나만)
4. PDP·ICE (permutation importance만 유지)
5. 피크 정의 민감도(상위 2.5%·10%)

**절대 삭감 금지**: 베이스라인 비교, 선정근거, FN·FP 조건 분석, 재현성, 블라인드 점검, 설문 캡처

---

## 8. 주최측 답변이 오면

- 예측 구간이 확정되면: A1 교체, 해당 예측거리를 2장 주 결과로 올리고 나머지는 곡선에서 보조로 남긴다
- 정량지표가 공통 테스트 기반이면: 해당 형식의 예측 파일 생성을 Phase 5 최우선 작업으로
- 전력 단위·구간 경계가 확인되면: A3·A4 교체, 4장 비용 분석을 금액 기반으로 확장
- 모든 변경은 `DECISIONS.md`에 기록하고 동결된 모델과 평가 프로토콜은 바꾸지 않는다

---

## 9. 세션 종료 시 보고 형식

```
[Phase N / 날짜]
완료:
핵심 수치: (CI 포함)
완료 기준 충족 여부:
막힌 점 / 사람 확인 필요:
다음 세션 할 일:
```
