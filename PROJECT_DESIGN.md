# 과제 ⑤ 전력사용량 예측 및 최대피크 위험조건 분석 설계서

설계 기준 2026-09-23 · 실행 정책 갱신 2026-09-24

제6회 K-인공지능 제조데이터 분석 경진대회 일반국민·대학(원)생 부문.
본 문서는 사용자 제공 설계를 실행 가능한 기준으로 정리한다. 작업 규칙·A1~A8은 `CLAUDE.md`, 사전 증거는 읽기 전용 `verification/`을 따른다.
`CLAUDE.md` 5·6절은 이 문서 9·12절로 대체한다. 이번 사용자의 무개입 지시가 종전 사람 확인 단계보다 우선한다.
알려지지 않은 개인 응답·공장 계약정보를 만들어 내거나 미래 일정을 완료로 표시하지 않는다.

## 1. 목적과 범위

공장 15분 전력으로 피크 발생 여부, 초과 크기, 불확실성을 예측한다. 현업 CBL 준용 방식과 persistence에 비교하고 2026 요금 시간대의 운영 의미를 분석한다.

| 요구 | 대응 | 장 |
| --- | --- | --- |
| 지정 시간구간 예측 | T1 한 시간 뒤 15분, T2 익일 최대, 예측거리 곡선 | 2 |
| 오차가 큰 생산조건 | 조건별 FN·FP, 저녁 실패 사례, 중요변수 | 3 |
| 피크 발생 주요 조건 | 에피소드 특성, 생산·잔차 분해 | 3 |
| 일정·설비 가동 조정 제안 | 확률 경보, cost-loss, 부하 이동 가정 | 4 |

성공 목표는 CBL과 persistence 대비 피크 MAE 개선의 CI, 상위 예측 구간 q95 커버리지 또는 부족 원인, 저녁 FN과 요금 시간대 비중, `python run_all.py` 재현이다. 성공 여부는 결과로 판정하며 미달을 숨기지 않는다.
설비별 최적화, 실제 절감액, 계절 간 일반화 입증, 인과적 효과는 범위 밖이다.

## 2. 원칙

관측 가용 시각 검증, 사전 채택 기준, 현업 기준선, 중복 기능 제거, 단순성 우선, YAML 설정과 자동 산출물을 지킨다.
원점은 t 구간 종료 직후이므로 `y(t)`의 관측 시각은 원점 이하이다. 설계 초안의 `< 원점`과 `y(t)` 충돌은 `≤ 원점`으로 해소한다. 학습 목표와 다음 검증 원점의 경계는 엄격한 `<`를 적용한다.

## 3. 데이터

1. CSV 원본 SHA-256 기록. 날짜별 24행 순서가 확인될 때만 시간 오류 48행을 00~23시로 복원한다. `time_repaired` 표시, 해당 행 및 그 관측에 의존하는 특징·목표 제외.
2. 15·30·45·60분 열을 HH:15, HH:30, HH:45, (HH+1):00 종료 시각으로 전개한다. 단위와 경계는 A3·A4 가정이다.
3. 0값은 유지하고 연속 길이·시간대·요일을 기록한다. IQR 이상치·결측 플래그는 진단용이며 전체 자료 통계로 예측을 전처리하지 않는다.
4. 평균 열과 네 값의 평균·합의 일치 여부는 단위의 단서일 뿐 단위 확정 근거가 아니다.
5. `data/processed/power_15min.parquet`에 전력·품질 플래그·생산·기상·인원 원자료를 보존한다.

| 변수 | 가용 시점 | 예측 | 사후 분석 |
| --- | --- | --- | --- |
| 15분 전력 | 구간 종료 직후 | 과거 및 원점 관측 | 가능 |
| 시간 생산량 | 시간 종료 후 | 완료 시간만 | 동시간 포함 |
| 기상 실측 | 사후 | 제외 | 가능 |
| 공장인원·인건비 | 불명확 | 제외 | 참고 |
| 같은 시간 평균 전력 | 목표와 중첩 | 제외 | 단위 진단만 |
| 달력·공휴일 | 사전 | 가능 | 가능 |

공휴일 상수: 1/1, 2/11~13, 3/1, 5/5, 5/19, 6/6, 8/15~16. 5/1은 별도 근로자의 날 플래그. 휴일 전후일, 휴일·주말 사이 평일, 주말 또는 공휴일을 산출한다. 정부 발표 확인 근거는 `docs/SOURCES.md`에 기록한다.
분할은 사전 검증과 동일한 원점 2021-08-09 09:45 경계로 고정한다. 피처를 바꿔 유효 행 수가 달라져도 테스트 경계를 재계산하지 않는다. 개발 내부 확장 학습 3폴드 및 예측거리 간격을 쓴다.

## 4. 과제와 목표

| ID | 원점 | 목표 |
| --- | --- | --- |
| T1 | 매 15분 구간 종료 직후 | h=4, 한 시간 후 15분 |
| T1-H | 동일 | h=1,4,16,96 직접 모델 |
| T2 | 전일 23:45 | 익일 종료 구간 96개의 최대 |

τ는 각 학습 구간 전력의 95백분위, 피크는 y>τ이다. 15분 연속 위치를 에피소드로 묶으며 결측 시각·평가 구간 경계에서 끊는다. 겹치는 실제·경보 에피소드를 일대일 매칭한다.
운영 관리 목표 T 기본값은 τ이다. 초안 YAML의 0.975 기본값 충돌을 0.95로 바로잡고 0.975는 민감도에만 둔다.

## 5. 특징

원점 전력과 1~7·96·672시차, 목표 같은 시각 전일·전주, 최근 1·4·16·96구간 평균·최대·표준편차 및 4구간 기울기, 피크 경과·7일 피크 수, 목표 달력, 완료된 생산 1~3시간·전일 합계·7일 같은 시간 평균, CBL와 당일 보정, 구 요금 시간대가 대상이다.
각 특징의 실제 의존 관측 시각과 복원 여부를 추적한다. h=96에서 목표 전일은 원점과 같아도 그보다 늦은 자료는 금지한다. 신 요금은 평가에만 사용한다.

## 6. 모델

| 계열 | 모델 |
| --- | --- |
| Persistence | 최근값, 원점 1시간 전 같은 위치, 최근 4개 평균 |
| 계절 나이브 | 목표 전일, 전주, 둘의 평균 |
| CBL 5종 | Mid6/10, Max4/5, 휴일 H-Mid4/6, 앞 두 방식의 당일 보정 |
| 점예측 | LightGBM, 공휴일 포함 여부, 피크 가중 1·2·4 |
| 분위수 | α=0.1·0.5·0.9·0.95·0.975, 교차 정렬 |
| 보정 | 전역 A, 중앙 예측 기준 50·90백분위 구간별 B |
| 위험 | 분위수 선형 보간 초과 확률, 채택 시 기대 초과량 |

CBL은 전력거래소 별표28 준용이다. 참고일은 목표일 이전이며 원점에 관측 가능해야 한다. 누락·복원 관측 제외. DR 참여·비정상근무일의 공식 자료 부재로 실제 정산과 동일하다고 주장하지 않는다. 당일 보정은 원점 직전 3시간만 쓴다.
계열 대표는 개발 전체 MAE 최소. LightGBM 소규모 그리드는 YAML과 결정 기록에 고정한다. 기대 초과량은 초과 확률×초과 조건부 크기이며 부족 표본에서는 단순 기준으로 대체한다.
마지막 검증에서 보정과 채택평가를 동시에 하지 않는다. 각 폴드 안에서 적합·보정·평가를 분리한다. 시계열의 비독립성 때문에 conformal의 분포무관 커버리지 보장을 주장하지 않는다.
최종 순서: 피크 MAE CI → 에피소드 F1 CI → 위치 FP → 상위 q95 커버리지 오차 → 모델·특징 수. CI가 겹치면 다음 지표를 적용한다.
선택 확률 분류 B와 파운데이션 비교는 이번 기본 실행에서 끈다. 딥러닝은 추가하지 않는다.

## 7. 평가

피크 위치 MAE, 에피소드 F1, 위치 FP, 전체·상위 구간 α=.9/.95 커버리지 오차를 주 지표로 한다. 에피소드 최대 시점·크기 오차, Brier, PR-AUC, 신뢰도, pinball, 전체 MAE·RMSE·0 제외 MAPE와 느슨한 발생일 F1을 보조로 둔다.
날짜 블록 1000회 쌍별 부트스트랩으로 95% 백분위 CI를 산출한다. 재표집된 서로 다른 날짜를 연속 에피소드로 이어 붙이지 않는다.
FVA는 CBL 대표 → persistence 대표 → 기본 LGBM → 공휴일 → 가중 → 분위수 A → B → 최종을 비교하며 각 단계의 채택·기각을 보인다. h=1·4·16·96에서 기준선 3계열 대비 CI와 최대 유효 예측거리를 기록한다.

| 선택 기법 | 개발 채택 조건 |
| --- | --- |
| 공휴일 | 휴일·전후일 MAE 개선 CI 하한>0, 전체 악화 없음 |
| 피크 가중 | 피크 MAE 개선, FP 증가≤20% |
| Mondrian | 상위 q95 커버리지 오차 감소, 전체 pinball 악화≤5% |
| 기대 초과량 | 실제 에피소드 순위상관 CI 하한>0 |
| 확률 분류 B | 에피소드 F1 또는 Brier 개선 CI 하한>0 |

사전 고정 파일은 `outputs/logs/adoption_criteria.md`, `eval_protocol.md`이다. 동결 날짜 이후 `final_test.csv` 1회 생성. 개발 결과는 `dev_*`로 구분한다.

## 8. 해석과 의사결정

오류 분해는 시간대 00~08·08~16·16~24 대리교대, 요일, 월, 공휴일, 학습 생산량 5분위, 구 요금 시간대별이다. 저녁 미탐과 적중 직전 4시간 전력·생산 추세를 비교하고 대표 사례 최대 3개를 그린다. 예측거리별 permutation importance 상위10, 선택 PDP·ICE 상위3을 제공한다.
분석용 에너지 기준선은 학습 기간 동시간 생산·기온·시각·요일로 적합한다. 기준선 자체가 τ 초과면 생산 연관형, 아니면 잔차 초과형으로 나눈다. 생산 기인·과다 사용이라는 원문 이름은 인과를 뜻하지 않는 사후 분류라는 주석을 붙인다. 시간 생산을 15분 자료에 4번 중복 합산하지 않는다. 원단위는 미확인 전력 단위의 대리지표다.

출력: origin,target_time,horizon,q50,q95_cal,p_exceed,exp_exceed(채택 시),margin=T−q95_cal,tariff_band_2026,alert_level.
경보: p_exceed>r=C/L 또는 margin<0. h16 검토, h4 조치. 1/1·2/2·2/3 연속 확인, 운전시간당 FP·에피소드 적중·선행시간, 준비15·30·60분을 비교한다.
REV는 기후평균 대비 상대 경제가치와 날짜 CI다. 요금은 실제 계약종별 단가 미확인일 때 시간대 및 명시적 순서 가중 시나리오만 제공한다. 이를 공식 비용 비중·원화 절감으로 부르지 않는다. 2021·2026 YAML에 출처, 미확인 상태를 기록한다. 2026 개편의 계절·주말 예외를 구분한다.
부하 이동은 생산10·20·30%와 학습 생산 계수로 근사한다. 목적지11~15시 중 예측 여유가 큰 시간이라는 사후 가정과 원점+준비시간 이후에만 가능한 운영 조건을 구분한다. 저녁 경보로 지난 낮에 이동한 성과를 실행 가능한 절감으로 계산하지 않는다.
디맨드 컨트롤러의 15분 안 반응형 제어와 1~4시간 선제 예측은 보완 관계다. 조치 후 효과는 CBL 기반 반사실 비교를 제안하되 인과 효과 검증이라고 부르지 않는다.
12월 부재·9월 중순까지 자료, 단위·경계 미확인, 설비·제품·교대 미관측, 테스트 사전 열람, 학습 분포와 평가 입력 거리 한계를 보고한다.

## 9. 코드 구조와 실행

`configs/`: default와 두 tariff YAML. `data/raw/`: 원본. `data/processed/`: parquet.
`src/`: data,holidays,targets,features,split,training,evaluate,bootstrap,viz.
`src/models/`: baselines,cbl,lgbm_point,lgbm_quantile,conformal,peak_prob,expected_exceedance.
`src/analysis/`: errors,horizon_curve,importance,energy_baseline,alerting,decision,tariff,simulate_shift,applicability.
`tests/`: 누수,CBL,보정,에피소드,분석,실행잠금. `outputs/`: figures,tables,predictions,logs,models.
`report/`, `slides/`, `submission/`는 제출 준비 산출물. `prototype/`은 기존 시제품이며 대회 결과와 분리한다.

핵심 API: build_features(df,origins,horizon,cfg)→(X,max_observed_time); cbl_predict(df,target_times,method,same_day_adjust,origin_times); fit/apply_conformal; match_episodes; evaluate_all.
실행 흐름은 data → features/baselines/point/quantile/risk/development → 날짜 잠금 final → analysis → report → package이다. `--only`와 `--from`으로 단계 선택, 해시 검증 캐시를 쓴다. 특징·모델 단계는 동일 개발 엔진에서 묶어 수행한다. CPU 30분은 목표이며 실제 시간을 기록한다.

## 10. 산출물

| ID | 내용 |
| --- | --- |
| F1-1~3 | 월·요일·시각 분포, 품질 타임라인, 생산·전력·기저부하 |
| T1-1~2 | 변수 가용시점, 누수 규칙·분할 |
| T2-1~2 | FVA, 최종 선정 근거 |
| F2-1~4 | 예측거리, TP·FP, 커버리지, 시점·크기 오차 |
| F3-1~4 | 조건 FN·FP, 저녁 사례, h별 중요변수, 피크 유형 |
| F4-1~5 | 운영 흐름, 경보 선행시간, REV, 요금 재평가, 이동 시뮬레이션 |
| T5-1 | 기법별 수치 변화·채택 여부 |
| T6-1 | 실행 단계·소요시간·산출물 |

각 장 Markdown 초안과 링크 가능한 HTML 로드맵을 제공한다. 그림·표·보고서 수치는 코드로 생성한다. PPT·PDF·hwpx 및 개인 설문 캡처는 실제 파일 존재와 검사 통과 전에는 완료 표시하지 않는다.

## 11. 위험과 대응

CBL이 강하면 최선 기준선으로 그대로 보고한다. 상위 q95 커버리지가 부족하면 표본 수·오차·한계를 정량 공개한다. 공휴일 CI가0을 포함하면 기각한다. 익일 우위가 없으면 유효 시간 범위만 주장한다. 단가 미확인은 시간대·순서가중으로 제한한다. 일정 지연 시 아래 선택 기능을 순서대로 줄인다.

## 12. 일정

| 단계 | 예정 | 완료 기준 |
| --- | --- | --- |
| 0 기반 | 9/23~24 | 기존 증거 피크 MAE 감사 재현, 구조·테스트·원본 보존 |
| 1 데이터 | 9/25~27 | 데이터 진단·1장 전 항목 |
| 2-1 기준선 | 9/27 | CBL5종, 대표 및 기준 고정 |
| 2-2 점예측 | 9/28~29 | 개발 CV·공휴일·가중 채택표 |
| 2-3 분위수 | 9/29~30 | A/B 커버리지 |
| 2-4 위험 | 9/30 | 확률·초과량 채택 |
| 2-5 거리 | 9/30~10/1 | h1·4·16·96,T2 |
| 2-6 동결 | 10/1 18시 | 최종 manifest와 테스트 1회 |
| 3 분석 | 10/1~3 | 조건 FN·FP, 저녁 실패, 피크 유형 |
| 4 활용 | 10/3~4 | 경보·요금·이동·4/5장 |
| 5 재현 | 10/4~6 | 새 환경 재실행,6장,예측 파일,hwpx 인계 |
| 6 발표 | 10/6~7 | PDF·PPT |
| 7 제출 | 10/7 | 블라인드 검사·포털 완료 증거 |
| 예비 | 10/8 | 문제 대응 |

완료 기준을 충족하면 개발 작업은 앞당긴다. 테스트 날짜 잠금은 유지한다.
삭감 순서: 파운데이션→확률B→τ민감도→기대초과량→h16→PDP/ICE→이동비율민감도→피크시점·크기오차.
삭감 금지: CBL,FVA,Mondrian 또는 실패정량,저녁 FN,2026 요금,조건 FN·FP,재현성,블라인드,설문캡처.

## 13. 무개입 진행과 인계

사용자 지시에 따라 10/5~6 hwpx 작업 전까지 반복 승인을 요청하지 않는다. 공휴일·공개 요금 근거는 에이전트가 조회한다. 준비시간30분은 가정으로 고정하고15·60분 민감도를 제공한다. 동결 승인은 사전 위임으로 기록하되 10/1 날짜 잠금을 지킨다.
문의 문안은 준비하되 수신처·발신 계정이 확인되지 않으면 미발송을 기록한다. 만족도 설문은 본인의 평가를 대신 지어낼 수 없으며 완료 캡처가 없으면 제출 준비 미완료로 남긴다. 계약종별·단가를 추정 입력하지 않는다. 포털 제출·완료 화면도 실제 수행 전에는 미완료이다.
현재 작업의 파일별 충족 상태는 `PROGRESS.md`, 차이·결정은 `DECISIONS.md`, 증거 출처는 `docs/SOURCES.md`, 인계 항목은 `submission/HANDOFF.md`를 따른다.
